<Window x:Class="AikoOS.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:live2d="clr-namespace:AikoOS.Live2D.Controls;assembly=AikoOS.Live2D"
        xmlns:views="clr-namespace:AikoOS.App.Views"
        xmlns:viewModels="clr-namespace:AikoOS.App.ViewModels"
        Title="{Binding ApplicationName, Mode=OneWay}"
        Width="1000"
        Height="650"
        MinWidth="800"
        MinHeight="500"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <DataTemplate DataType="{x:Type viewModels:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:ChatViewModel}">
            <views:ChatView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:MemoryViewModel}">
            <views:MemoryPage/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:SettingsViewModel}">
            <views:SettingsPage/>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="#DDDDDD"
                BorderThickness="0,0,0,1"
                Padding="20,0">

            <DockPanel>

                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <TextBlock Text="{Binding ApplicationName, Mode=OneWay}"
                               FontSize="24"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>

                    <TextBlock Text="{Binding ApplicationVersion, Mode=OneWay}"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"/>

                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">

                    <Button Content="Home"
                            Command="{Binding NavigateHomeCommand}"
                            Width="100"
                            Margin="0,12,8,12"/>

                    <Button Content="Chat"
                            Command="{Binding NavigateChatCommand}"
                            Width="100"
                            Margin="0,12,8,12"/>

                    <Button Content="Memory"
                            Command="{Binding NavigateMemoryCommand}"
                            Width="100"
                            Margin="0,12,8,12"/>

                    <Button Content="Settings"
                            Command="{Binding NavigateSettingsCommand}"
                            Width="100"
                            Margin="0,12,0,12"/>

                </StackPanel>

            </DockPanel>
        </Border>

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Khu vực avatar Aiko -->
            <Border Grid.Column="0"
                    Margin="12"
                    BorderBrush="#DDDDDD"
                    BorderThickness="1"
                    CornerRadius="12">

                <Grid>
                    <live2d:Live2DControl x:Name="Live2DView"/>

                    <TextBlock Text="Aiko"
                               Margin="16"
                               FontSize="20"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Top"
                               IsHitTestVisible="False"/>
                </Grid>

            </Border>

            <!-- Nội dung tab -->
            <Border Grid.Column="1"
                    Margin="0,12,12,12"
                    BorderBrush="#DDDDDD"
                    BorderThickness="1"
                    CornerRadius="12">

                <ContentControl Content="{Binding CurrentViewModel}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch"/>

            </Border>

        </Grid>

        <Border Grid.Row="2"
                BorderBrush="#DDDDDD"
                BorderThickness="0,1,0,0">

            <DockPanel Margin="12,0">

                <Button Content="Test Live2D"
                        Width="110"
                        Height="24"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right"
                        Click="TestLive2DButton_Click"/>

                <TextBlock x:Name="Live2DStatusText"
                           Text="Live2D: Loading"
                           Margin="20,0,0,0"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Right"/>

                <TextBlock VerticalAlignment="Center">
        <Run Text="Environment: "/>
        <Run Text="{Binding EnvironmentName, Mode=OneWay}"/>
                </TextBlock>

            </DockPanel>

        </Border>
    </Grid>
</Window>