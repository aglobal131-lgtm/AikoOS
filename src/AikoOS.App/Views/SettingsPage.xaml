<UserControl x:Class="AikoOS.App.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="32">
        <StackPanel Width="420"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <TextBlock Text="Settings"
                       FontSize="32"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Assistant name"
                       Margin="0,24,0,6"
                       FontWeight="SemiBold"/>

            <TextBox Text="{Binding AssistantName,
                                    UpdateSourceTrigger=PropertyChanged}"
                     Height="36"
                     Padding="8,5"/>

            <CheckBox Content="Start AikoOS with Windows"
                      IsChecked="{Binding StartWithWindows}"
                      Margin="0,18,0,0"/>

            <CheckBox Content="Minimize AikoOS to the system tray"
                      IsChecked="{Binding MinimizeToTray}"
                      Margin="0,10,0,0"/>

            <Button Content="Load settings"
                    Command="{Binding LoadCommand}"
                    Height="38"
                    Margin="0,22,0,0"/>

            <Button Content="Save settings"
                    Command="{Binding SaveCommand}"
                    Height="38"
                    Margin="0,10,0,0"/>

            <ProgressBar IsIndeterminate="True"
                         Height="4"
                         Margin="0,12,0,0"
                         Visibility="{Binding IsBusy,
                                              Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <TextBlock Text="{Binding StatusMessage, Mode=OneWay}"
                       Margin="0,16,0,0"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"/>

        </StackPanel>
    </Grid>
</UserControl>