<UserControl x:Class="AikoOS.App.Views.Brain.BrainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="400"
             MinHeight="300">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Trò chuyện với Aiko"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <TextBox Grid.Row="1"
                 Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Padding="12"
                 MinHeight="140"/>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Margin="0,12,0,12"
                   Opacity="0.75"/>

        <Button Grid.Row="3"
                Content="Gửi cho Aiko"
                Command="{Binding SendCommand}"
                HorizontalAlignment="Right"
                MinWidth="130"
                Padding="14,8"/>
    </Grid>
</UserControl>